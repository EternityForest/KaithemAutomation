from kaithem.src import devices as _devices

# Use all_devices[DevName] or for i in all_devices to get device objects.
# Devices will be iot_devices Device class objects.
all_devices = _devices.DeviceNamespace()
