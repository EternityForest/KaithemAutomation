allow-origins: ['*']
allow-xss: false
auto-reload: false
auto-reload-interval: 5.0
dont-show-in-index: false
no-header: false
no-navheader: true
require-method: [GET, POST]
require-permissions: []
resource-type: page

#End YAML metadata, page body mako code begins on first line after ---
---
<%!
#Code Here runs once when page is first rendered. Good place for import statements.
__doc__= "This shows the list of all active modules. WARNING: This will try to display the full content of all modules and resources. Could be pretty large"
from src import modules_state as m
from src import util
%>
<%
#Python Code here runs every page load
%>
<h2>ActiveModules</h2>
<div class="sectionbox">
   %for i in m.ActiveModules:
    <h3>${i}</h3>
    
    <ul>
        %for j in m.ActiveModules[i]:
        <li>${j|h}</li>
        %endfor
    </ul>
    %endfor
    </div>