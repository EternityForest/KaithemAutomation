{
    "body": "<%!import os%>\r\n\r\n<%kaithem.message.postMessage(\"/test\", str(os.urandom(10)) )%>\r\nThis page sends a message to the message bus every time it is viewed. It sends a random string to /test",
    "no-header": false,
    "no-navheader": true,
    "require-method": [
        "GET",
        "POST"
    ],
    "require-permissions": [],
    "resource-type": "page"
}