{
    "allow-origins": [
        "*"
    ],
    "allow-xss": false,
    "auto-reload": false,
    "auto-reload-interval": 5.0,
    "body": "<%!\r\n#Code Here runs once when page is first rendered. Good place for import statements.\r\n__doc__= \"Show the state of the scheduler in scheduling.py\"\r\nfrom src import scheduling\r\nimport yaml\r\n%>\r\n<%\r\n#Python Code here runs every page load\r\n%>\r\n<h2>Title</h2>\r\n<div class=\"sectionbox\">\r\nCurrent Tasks(function, module, runtime):<br><pre>${yaml.dump([[i.f().__name__,i.f().__module__, kaithem.string.usrstrftime(i.time)] for i in scheduling.scheduler.tasks])|h}</pre><br><br>\r\nRecurring Tasks(function, module, interval):<br><pre>${yaml.dump([[i.f().__name__,i.f().__module__, i.interval] for i in scheduling.scheduler.repeatingtasks])|h}</pre>\r\n</div>",
    "dont-show-in-index": false,
    "no-header": false,
    "no-navheader": true,
    "require-method": [
        "GET",
        "POST"
    ],
    "require-permissions": [],
    "resource-type": "page"
}